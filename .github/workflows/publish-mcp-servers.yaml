name: Publish MCP server images

on:
  push:
    branches: [main]
    paths:
      - "mcp-servers/socrata-mcp/Containerfile"
      - ".github/workflows/publish-mcp-servers.yaml"
  pull_request:
    paths:
      - "mcp-servers/socrata-mcp/Containerfile"
      - ".github/workflows/publish-mcp-servers.yaml"
  workflow_dispatch:

concurrency:
  group: publish-mcp-servers-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write

jobs:
  socrata-mcp-server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: docker/setup-qemu-action@v4

      - uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository_owner }}/socrata-mcp-server
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=long

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./mcp-servers/socrata-mcp
          file: ./mcp-servers/socrata-mcp/Containerfile
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
