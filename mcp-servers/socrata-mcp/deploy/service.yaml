apiVersion: v1
kind: Service
metadata:
  name: socrata-mcp-server
  labels:
    app: socrata-mcp-server
spec:
  selector:
    app: socrata-mcp-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
