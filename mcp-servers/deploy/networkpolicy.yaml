apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: socrata-mcp-server-allow-all
spec:
  podSelector:
    matchLabels:
      app: socrata-mcp-server
  policyTypes:
    - Ingress
  ingress:
    - from:
        - ipBlock:
            cidr: 0.0.0.0/0
