# No custom certificate is specified below, so the OpenShift router applies
# its default wildcard certificate for *.apps.cloud.rt.nyu.edu.
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: flint-mcp-server
  labels:
    app: flint-mcp-server
spec:
  host: flint-mcp.apps.cloud.rt.nyu.edu
  to:
    kind: Service
    name: flint-mcp-server
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
