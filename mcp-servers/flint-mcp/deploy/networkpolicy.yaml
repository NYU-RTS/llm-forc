apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: flint-mcp-server-allow-all
spec:
  podSelector:
    matchLabels:
      app: flint-mcp-server
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector: {}
