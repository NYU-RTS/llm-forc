apiVersion: v1
kind: Service
metadata:
  name: flint-mcp-server
  labels:
    app: flint-mcp-server
spec:
  selector:
    app: flint-mcp-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
